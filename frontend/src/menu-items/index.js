// project import
import pages from './pages';
import dashboard from './dashboard';
import support from './support';
import portfolio from './portfolio';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, pages, portfolio, support]
};

export default menuItems;
